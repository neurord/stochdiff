package org.catacomb.serial.state;


public interface ComplexState {

    // defines a state as having its own substates
    // (eg that need serializing) as opposed to references to
    // states elsewhere.

}
